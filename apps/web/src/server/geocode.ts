/**
 * Suburb lookup. The table lives in @gymgo/demo-data so the mobile app uses
 * the same one; see packages/demo-data/src/places.ts.
 */

export { PILOT_PLACES, geocode, suburbSlug, type GeocodeResult, type Place } from '@gymgo/demo-data';
