{
  "name": "@gymgo/au-data",
  "version": "0.1.0",
  "private": true,
  "description": "Gyms in six more Australian cities, from OpenStreetMap (ODbL). Map-only: no prices, hours only where mapped.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "fetch": "python3 scripts/fetch.py",
    "generate": "python3 scripts/generate.py"
  },
  "dependencies": {
    "@gymgo/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^5.0.1"
  }
}
