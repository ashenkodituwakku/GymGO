{
  "name": "@gymgo/eu-data",
  "version": "0.1.0",
  "private": true,
  "description": "Gyms in fifteen European cities, from OpenStreetMap (ODbL). Map-only: no prices, hours only where mapped.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "fetch": "python3 scripts/fetch.py",
    "generate": "tsx scripts/generate.ts"
  },
  "dependencies": {
    "@gymgo/domain": "workspace:*",
    "@gymgo/osm": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.3",
    "vitest": "^5.0.1"
  }
}
