{
  "name": "@gymgo/melbourne-data",
  "version": "0.1.0",
  "private": true,
  "description": "Real inner-Melbourne gyms, each fact with its public source and check date. Locations from OpenStreetMap (ODbL).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@gymgo/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^5.0.1"
  }
}
