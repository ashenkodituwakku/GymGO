{
  "name": "@gymgo/osm",
  "version": "0.1.0",
  "private": true,
  "description": "What counts as a gym on OpenStreetMap, and how a mapped gym becomes a map-only GymGO record. Shared by the city data generators and the server's area search.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@gymgo/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^5.0.1"
  }
}
